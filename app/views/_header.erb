<nav>
  <h1>Ask DBC</h1>

  <% if session[:current_user] %>
    <a href="/<%= session[:current_user] %>"> Profile</a>
    <a href='/signout'>Sign Out</a>
  <% else %>
    <a href='/auth'>Log in With Google</a>
  <% end %>
</nav>